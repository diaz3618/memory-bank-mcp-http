# =============================================================================
# Docker Hub — Build + Push Docker Image (Private)
# =============================================================================
# Branch: main
#
# Triggers:
#   - Push to main
#   - Push tag v*-http-pg-redis
#   - Manual dispatch
#
# Tag convention (branch-separation-policy):
#   - <version>-http-pg-redis  (e.g., 1.8.0-http-pg-redis)
#   - latest-http              (rolling latest for HTTP variant)
#
# Required secrets:
#   - DOCKERHUB_USERNAME
#   - DOCKERHUB_TOKEN (PAT with read/write)
# =============================================================================

name: Docker — Docker Hub

on:
  push:
    branches: [main]
    tags: ['v*-http-pg-redis']
  workflow_dispatch:

env:
  IMAGE_NAME: diaz3618/memory-bank-mcp

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            # On tag push: v1.8.0-http-pg-redis → 1.8.0-http-pg-redis
            type=match,pattern=v(.*),group=1,enable=${{ startsWith(github.ref, 'refs/tags/') }}
            # On branch push: latest-http
            type=raw,value=latest-http,enable=${{ github.ref == 'refs/heads/main' }}
            # Git SHA for traceability
            type=sha,prefix=

      - name: Build and push — HTTP transport
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.http
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64
