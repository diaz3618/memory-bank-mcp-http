[install]
# Use the latest compatible versions
optional = true
exact = false

# Build settings
[build]
target = "node"
minify = true
sourcemap = true

# Development settings
[dev]
hot = true

[test]
root = "src"

# Multiple patterns to exclude from the coverage report
coverageExclude = [
  "repos/**",
  "node_modules/**"
]